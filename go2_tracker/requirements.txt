# Deep Re-ID Tracking Requirements
# Install with: pip install -r requirements.txt

# Core dependencies (likely already installed)
opencv-python>=4.8.0
numpy>=1.24.0

# YOLO11 (Ultralytics)
ultralytics>=8.0.0

# Deep Learning Re-ID (using ResNet from torchvision)
torch>=2.0.0
torchvision>=0.15.0

# Note: GPU support requires CUDA-compatible PyTorch installation
# For GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
